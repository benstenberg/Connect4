
class Theme {
   constructor( color, playerToken, computerToken ) {
      this.color = color;   // String of #DDDDDD game color
      this.playerToken = playerToken; // token id
      this.computerToken = computerToken; // token id
   }
}

module.exports = Theme;
